# Legacy Track Assets

The original static dashboard used PNG files stored in this folder.

The Next.js dashboard now serves generated SVG track maps from circuit geometry
data, so the PNG files have been removed. Keep this folder only if explicit
fallback assets are reintroduced later.
